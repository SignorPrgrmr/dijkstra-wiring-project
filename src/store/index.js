import { createStore } from 'vuex'

export default createStore({
    state: {
        serverUrl: "http://localhost:8080",
    },
    mutations: {
    },
    actions: {
    },
    modules: {
    },
    getters: {
    }
})
